<section class="shelter-list">
  <%= link_to 'Create Shelter', "/shelters/new" %>
  
  <ul>
    <% @shelters.each do |shelter| %>
      <li id="shelter-list-item-<%= shelter.id %>"><%= shelter.name %></li>
    <% end %>
  </ul>
</section>
